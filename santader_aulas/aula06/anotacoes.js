
//! Condicionais simples //
/**IF(SE ) E Else(se não)  if(CONDIÇÃO){}*/
//! Condicionais ANINHADAS //
/**IF(SE ) E Else IF(se não) ELSE()  if(CONDIÇÃO){}*/

//**switch case */
//? sintaxe switch(Variavél){ case 1 : break}

//**operador ternário */
//? :}

//** Estruturas condicionais elaços de repetiçoes é alteraro fluxo de execução */
//?NÃO controlada: EU NÃO SEI QUANTAS VEZES SÉRA EXECUTADA, quando é necessário repetir e não sabemos quantas vezes será necessaria//
//? while(enquanto) e do while//

//? for//

//arrays : coleçoes de dados //

//*DRY - dont repeat youself*/
//?parametro é variavél escopada só existe dentro do escopo//
/templetade string `${variavel}`/
//* paremetro padrão é default (caso  o parametro não seja passado o js ultilizará o valor atribuido ) */
//* function saudação( nome aluno ="pedro", curso){}(O PARAMETRO NOME ALUNO JÁ ESTÁ SENDO ATRIBUIDO )*/

